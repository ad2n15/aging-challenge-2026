# Container

The Bionemo container (`bionemo-framework_nightly.sif`) is NOT stored in git (too large).

Copy it from the shared space:
```bash
cp /scratch/aazd1f17/shared_space/aging-challenge-2026/container/bionemo-framework_nightly.sif ~/aging-challenge-2026/container/
```
